= form_for [:admin, @double_yellow_card_description] do |f|
  - if @double_yellow_card_description.errors.any?
    #error_explanation
      %h2= "#{pluralize(@double_yellow_card_description.errors.count, "error")} prohibited this double_yellow_card_description from being saved:"
      %ul
        - @double_yellow_card_description.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
