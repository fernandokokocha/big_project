= form_for [:admin, @yellow_card_description] do |f|
  - if @yellow_card_description.errors.any?
    #error_explanation
      %h2= "#{pluralize(@yellow_card_description.errors.count, "error")} prohibited this yellow_card_description from being saved:"
      %ul
        - @yellow_card_description.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
